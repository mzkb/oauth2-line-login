server:
  port: ${APP_SERVER_PORT:9003}
  shutdown: graceful
spring:
  autoconfigure:
    exclude[0]: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
line:
  clientId: ${LINE_CLIENT_ID}
  clientSecret: ${LINE_CLIENT_SECRET}
  redirectUri: ${LINE_REDIRECT_URI}